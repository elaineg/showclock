# Round 2 — Jules

**1. Purpose clear in 5s? Yes (up from Partially).**
"Showclock" + "Know exactly how far behind you are — and what time everything starts now"
+ the "Agenda — one item per line" box + "Start show" button told me in ~4s: paste my run
of show, hit start, it tracks how far behind I am. The tab title now reads "paste-and-go
run-of-show timer" too. Nit: the visible H1 is STILL just "Showclock" — the word "timer"
lives in the subtitle/tab, not the headline. Clear enough now, so Yes, but barely.

**2. Valuable to me? Yes.**
Today I run my weekly AMA off a Notion run-of-show + my phone's stopwatch and do the
"are we behind?" math in my head. This does it for me: I pasted a loose AMA agenda, let
"Community AMA" overrun (+0:15 over the planned 1 min, band went solid RED), and it told me
the projected end shifted — zero math, no login, works on my 375px phone at the podium.
The live read-only link I can hand a co-host is exactly the thing I wanted.

**3. Advocacy: 8 (Δ +1 from round-1 7).**
Both my round-1 caps are addressed, so it clears the polite-7 line. Held off 9 only by the
leftover "snapshot view" line on the landing page (see below) + the H1 still omitting "timer".

**Contradiction (badge vs band):** RESOLVED. I forced a real overrun — current item band
RED ("+0:15 over the planned 1 min") AND the WHOLE-SHOW DRIFT badge went red with a real
number instead of a calm green "on time." They now agree. (One oddity: a 2-item test showed
"3 min ahead" tinted red — being ahead in alarm-red is slightly off, but that's a nuance,
not the lie I flagged.)

**Share-link copy:** MATCHES REALITY now. The control says "Shares a live read-only view,"
and opening it showed a banner "You're viewing a live read-only view…" with the timer
genuinely ticking (14:55→14:52 in 3s), read-only (no Back/Next). Honest. BUT the homepage
pre-start blurb still says "copy the URL to give a co-facilitator a snapshot view" — that
one stray "snapshot" wasn't updated to match. Minor, but fix it for consistency.

```json
{"tester": 0, "round": 2, "clarity": "Yes", "value": "Yes", "advocacy": 8,
 "topComplaints": ["homepage still says 'snapshot view' while in-app/share say 'live read-only view'", "visible H1 still 'Showclock' with no 'timer' word"],
 "priorConcernsAddressed": "some"}
```
