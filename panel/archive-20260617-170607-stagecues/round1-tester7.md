```json
{"tester":7,"name":"Aisha","clarity":"Yes","value":"Yes","advocacy":8,"biggest_blocker":"No behind/over-time state is easy to trigger — pill almost always reads 'ahead', so the one moment I most need it (running long) feels untested in the demo"}
```

# Aisha — Product designer, FigJam workshop facilitator

## Clarity — Yes
Five seconds in, I knew exactly what this was and that it was for me. "Showclock" + "Paste your run-of-show, hit Start, and always know if you're ahead or behind" is a clean, confident one-liner — the verb-first copy ("Paste… hit Start… always know") is the tone I trust. The placeholder agenda in the textarea (Welcome 5 / Demo - 20 min / 15 Q&A) doubles as a format example and quietly teaches me three input styles at once. "No accounts — the whole session lives in the URL" answered my privacy/setup question before I asked it. This is legible cold, no hand-holding needed.

## Value — Yes
Today I hand-build timeboxes in FigJam with a stopwatch widget per section and do the "are we behind?" math in my head while also running the room — which is exactly when my brain is most loaded. Showclock collapses that into one glanceable view: big countdown, current item name, a single ahead/behind pill, and a rundown showing planned -> projected end times for every remaining block. The "projected end 10:42 PM" line is the thing I actually care about and never have time to compute live. It parsed all my messy formats ("Warmup - 5 min", "Break 5", "15 Q&A") without complaint. Setup was ~10 seconds. That's a real save over my current ritual.

## Craft notes (I judge this hard)
The presenter view is calm. Generous whitespace, one dominant element (the clock), restrained type scale, and the rundown uses the right hierarchy — bold projected times, muted planned times, strikethrough on completed items, a soft green band on the current row. "Next" becomes "Finish" on the last item; small, but it's the kind of considered detail I advocate for. Copy-view-only-link works (verified; gives a co-facilitator a read-only state via the URL hash).

What holds it to an 8, not a 10: (1) The ahead/behind pill reads "ahead" almost constantly because the default start time sits in the past — I never naturally saw the BEHIND state, which is the single moment a facilitator needs this most. I want to trust the over-time color/urgency treatment and I couldn't see it without fiddling. (2) The "Copy view-only link" button gave no label-change confirmation ("Copied!") after click in my run — a tiny but real feedback gap for a tool you use under pressure. (3) The monospace projected-time column is functional but a touch utilitarian against the otherwise warm type.

## Advocacy — 8
I'd bring this up in my next workshop-ops Slack thread unprompted. It's the rare timebox tool that feels designed rather than assembled. Fix the behind-state visibility and add a copy confirmation and it's a 9.
