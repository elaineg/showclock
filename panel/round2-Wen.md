# Round 2 — Wen

**1. Purpose clear in 5s? YES.** "Showclock — Know exactly how far behind you are — and what time everything starts now." Paste an agenda, it tracks drift + reflows clock times live. Obvious for anyone running a timed session. The "planned → projected" rundown header is exactly the framing a data person wants.

**2. Valuable to me? YES.** Today I run my weekly 6-segment training off a Google Sheet with hand-math for "are we behind." Parse fidelity is STILL EXACT: I pasted mixed syntax — "Welcome - 3 min", "Segment 1: SQL basics - 12 min", bare "BigQuery joins 18", "Q&A 15" — all parsed to 3/12/18/22/15m, 75 min total, no silent reinterpretation. The drift math is now fully transparent: I watched a 1-min item overrun to +2:00 and the badge counted the cushion down 3→2→1 min ahead, 1:1 with real time, projected end creeping 8:22→8:24. I could reconcile every line. This beats my sheet.

**3. Advocacy: 9/10.** Δ from round-1 8 = **+1.** I'd bring this up at my next facilitator sync unprompted. Holds it back from 10: the **"Copy current link" button still gives NO "Copied" confirmation** — label never changes after click (clipboard did receive the link in my env; verified the click handler fired, so this is a real missing visual confirm, not an env artifact). Still no CSV export of the projected rundown — a nicety for my hygiene workflow, non-gating.

**Contradiction closed? YES.** The round-1 green-"on time"-badge-while-item-band-is-red contradiction is RESOLVED: when the item band goes RED the drift badge now turns red-tinted in sympathy AND always shows the honest whole-show number ("on time" / "X min ahead" / overrun), sub-labeled WHOLE-SHOW DRIFT so it reads as subordinate to the item band. "on time" on a red badge wasn't false — the whole show still projected ahead while the item overran a sliver of cushion; the two metrics are now visibly distinct and both truthful. Drift math is transparent and correct.

```json
{"tester": 0, "round": 2, "clarity": "Yes", "value": "Yes", "advocacy": 9, "topComplaints": ["Copy current link button shows no 'Copied' confirmation (label unchanged after click)", "No CSV export of the projected rundown"], "priorConcernsAddressed": "some"}
```
